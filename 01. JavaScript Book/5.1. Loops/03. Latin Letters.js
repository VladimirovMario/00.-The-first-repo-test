function latinLetters() {
  for (let i = 97; i <= 122; i++) {
    console.log(String.fromCharCode(i));
  }
}
latinLetters();

// function latinLetters2() {
//   for (let ch = `a`.charCodeAt(0); ch <= `z`.charCodeAt(0); ch++) {
//     console.log(String.fromCharCode(ch));
//   }
// }
// latinLetters2();
